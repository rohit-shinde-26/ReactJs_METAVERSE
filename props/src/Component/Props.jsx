import React from "react";
const Props =(Props) => {
    
    return(
       <>
        <h1>{Props.name}</h1>
        <h1>{Props.age}</h1>
       </>
    )
}
export default Props;

// Props are send parent to child component for the passing props import firstly on parent 